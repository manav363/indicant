"""
intelligence/backtest/
───────────────────────
Walk-forward backtesting engine for evaluating ML trading strategies.

Components:
    engine      — walk-forward backtest loop (run_backtest)
    metrics     — performance metrics (sharpe, sortino, max_dd, etc.)
"""
